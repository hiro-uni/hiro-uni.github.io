---
import Layout from '../layouts/Layout.astro';
import notfound from '../assets/not_found.png';
import { Image } from 'astro:assets';
---

<Layout title="404 Not Found">
	<div class="error-container">
		<div class="error-content">
			<h1 class="error-code">404</h1>
			<p class="error-message">お探しのページは見つかりませんでした。</p>

			<Image src={notfound} alt="404 Not Found" class="not-found-image"/>
			<p class="error-description">
				アクセスしようとしたページは削除。<br>もしくは、URLが変更された可能性があります。
			</p>

			<div class="actions">
				<a href="/" class="home-button">
					<i class="fas fa-home"></i> ホームに戻る
				</a>
			</div>
		</div>
	</div>
</Layout>

<style>
	.error-container {
		display: flex;
		align-items: center;
		justify-content: center;
		padding: 4rem 1rem;
		text-align: center;
	}

	.error-content {
		max-width: 500px;
	}

	.not-found-image {
		width: 100%;
		max-width: 500px;
		height: auto;
		margin: 2rem auto;
	}

	.error-code {
		font-size: 8rem;
		font-weight: 800;
		color: var(--secondary);
		margin: 0;
		line-height: 1;
		text-shadow: 4px 4px 0px rgba(179, 11, 84, 0.1);
	}

	.error-message {
		font-size: 1.5rem;
		font-weight: bold;
		margin: 1rem 0;
		color: var(--text);
	}

	.error-description {
		color: #666;
		margin-bottom: 2.5rem;
	}

	.home-button {
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		background-color: var(--secondary);
		color: white;
		padding: 0.8rem 2rem;
		border-radius: 50px;
		text-decoration: none;
		font-weight: bold;
		transition: transform 0.2s, background-color 0.2s, box-shadow 0.2s;
		box-shadow: 0 4px 15px rgba(179, 11, 84, 0.3);
	}

	.home-button:hover {
		background-color: #900944;
		transform: translateY(-2px);
		box-shadow: 0 6px 20px rgba(179, 11, 84, 0.4);
		color: white;
	}

	.home-button i {
		font-size: 1.1rem;
	}

	@media (max-width: 600px) {
		.error-code {
			font-size: 6rem;
		}
		.error-message {
			font-size: 1.2rem;
		}
	}
</style>
